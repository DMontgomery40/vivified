[project]
name = "vivi"
version = "0.0.1"
description = "Vivi — a tiny, typed kernel with polyglot plugins."

[tool.uv]
dev-dependencies = ["ruff>=0.6.0","mypy>=1.10.0","pytest>=8.2.0","httpx>=0.27.0"]

[tool.ruff]
line-length = 100
