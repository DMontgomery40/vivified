[project]
name = "vivi-cli"
version = "0.0.1"
description = "Vivi CLI"
requires-python = ">=3.11"
dependencies = ["typer>=0.12.5","vivi-core>=0.0.1"]

[project.scripts]
vivi = "vivified_cli.__main__:app"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
