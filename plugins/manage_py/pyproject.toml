[project]
name = "manage-py"
version = "0.0.1"
description = "Management plugin for Vivi (traits + plugin enable/disable)"
requires-python = ">=3.11"
dependencies = ["vivi-core>=0.0.1"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

