[project]
name = "hello-py"
version = "0.0.1"
description = "Example Python plugin for Vivified"
requires-python = ">=3.11"
dependencies = ["vivified-core>=0.0.1"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
